{
  "name": "sesimiz-ol-backend-deploy",
  "version": "1.0.0",
  "description": "Backend deployment for Railway",
  "private": true,
  "scripts": {
    "build": "cd backend && npm run build",
    "start": "cd backend && npm run start:production"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}